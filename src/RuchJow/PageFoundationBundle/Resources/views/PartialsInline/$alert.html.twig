{{ '<script type="text/ng-template" id="$alert.html">' }}
<article>
    <div class="modal-header" ng-if="title">
        <h1>[[ title ]]</h1>
    </div>
    <div class="modal-body">
        <div class="alert-msg [[ type ]]">
            <span>[[ msg ]]</span>
        </div>
    </div>
    <div class="modal-footer" ng-if="showOkBtn || showCancelBtn">
        <button class="btn btn-msg-ok" ng-click="ok()" ng-if="showOkBtn">
            <span>[[ btnOkCaption ]]</span>
        </button>
        <button class="btn btn-msg-cancel" ng-click="cancel()" ng-if="showCancelBtn">
            <span>[[ btnCancelCaption ]]</span>
        </button>
    </div>
</article>
{{ '</script>' }}